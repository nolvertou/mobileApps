fun main(){
	val myStr = "Hello World" 
    var firstCharInStr = myStr[0]
	var lastCharInStr = myStr[myStr.length-1]

    // Print Results
    println("First Character: $firstCharInStr\n Last Character: $lastCharInStr \n String Length: ${myStr.length} letters")
}
